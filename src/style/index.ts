import { createTheme } from '@mui/material/styles'

const themeDark = createTheme({
  palette: {
    background: {
      default: "#135040"
    },
    text: {
      primary: "#2e7d32"
    }
  }
});

export default themeDark;