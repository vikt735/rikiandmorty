import { makeStyles } from '@mui/styles';

const useStyleNoResult = makeStyles({
  noResults: {
    padding: '20px',
    textAlign: 'center',
    backgroundColor: '#135040',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  },
});
export default useStyleNoResult;