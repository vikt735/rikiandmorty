import { makeStyles } from '@mui/styles';

const useStyleFormSelect = makeStyles({
  formControl: {
    minWidth: '25ch',
    flex: '1 0',
    
  },
  label: {
    left: '-14px'
  }
});

export default useStyleFormSelect;

