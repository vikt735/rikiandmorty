import { makeStyles } from '@mui/styles';

const useStyleLoader = makeStyles({
  loader: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '150px',
  },
});

export default useStyleLoader;